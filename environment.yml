name: diffusion_modes_dnn_architecture
channels:
  - defaults
dependencies:
  - altair=3.2.0
  - flake8
prefix: C:\Users\Milosz\anaconda3\envs\diffusion_modes_dnn_architecture
# conda env export --from-history --file  environment.yml
